const Dashboard = async () => {
  return (
    <div className="flex-1 flex items-center justify-center h-full">
      <p className="text-[#ffffff20] text-sm [font-family:var(--font-mono)]">
        select a conversation
      </p>
    </div>
  );
};

export default Dashboard;
